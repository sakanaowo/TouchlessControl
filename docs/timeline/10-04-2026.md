# 10-04-2026 — TouchlessControl

### 00:00 — Project timeline khởi tạo

**Vấn đề:** AGENTS.md được copy từ project khác, chứa nhiều config không liên quan (GCP, Vertex AI, LangChain, env files).
**Nguyên nhân:** Reuse template chưa customize.
**Hành động:** Viết lại AGENTS.md cho TouchlessControl: cập nhật tech stack (MediaPipe, TFLite, OpenCV, pynput), conda env `sign`, known constraints thực tế (webcam, Qt/Wayland, feature vectors, CSV format). Loại bỏ GCP/cloud sections, notebook testing, env vars. Tạo folder `docs/timeline/` + `docs/ai/timeline.md`.
**Kết quả:** AGENTS.md phản ánh đúng project. Timeline tracking bắt đầu.
**References:** AGENTS.md, docs/ai/timeline.md

### 00:30 — Design review guided-gesture-collection

**Vấn đề:** Design doc không khớp với requirements đã resolve (7 gaps).
**Nguyên nhân:** Design được viết trước khi Q&A resolve 10 decisions.
**Hành động:** Cập nhật design doc: (1) Thêm ClassMenu component + label CSV auto-load, (2) Sửa state diagram thêm menu_open, (3) Cancel = discard entire buffer (bỏ "keep partial"), (4) Thêm RAM buffer vào CollectionManager, (5) Thêm frame skip logic, (6) Thêm timeout 10s → flush partial, (7) Multi-hand: on_frame(hands: list) ghi cả 2 tay, collected đếm frame. Cập nhật mermaid diagrams (architecture + state).
**Kết quả:** Design doc aligned với requirements. Sẵn sàng /execute-plan.
**References:** docs/ai/design/feature-guided-gesture-collection.md, docs/ai/requirements/feature-guided-gesture-collection.md

### 16:46 — Implement guided-gesture-collection (T1–T3)

**Vấn đề:** Cần implement toàn bộ guided gesture collection UI theo design doc.
**Nguyên nhân:** Thiếu ClassMenu, CollectionManager, overlay UI, balance indicator.
**Hành động:**

- T1.2: Tạo `utils/class_menu.py` — ClassMenu class: load labels từ CSV, Tab toggle, ↑/↓ navigate, Enter confirm, hot-reload, semi-transparent overlay draw.
- T1.3: Tạo `utils/collection_manager.py` — CollectionManager FSM (idle→countdown→recording→done), RAM buffer, quality gate ≥0.7, frame skip, multi-hand HandData, timeout 10s→flush partial, cancel→discard, auto-count existing CSV.
- T1.4: 47 unit tests (16 ClassMenu + 31 CollectionManager) — all pass.
- T2.1: Tích hợp vào app.py main loop — bỏ select_mode/logging_csv cũ, thay bằng ClassMenu + CollectionManager. Key: Tab/↑↓/Enter/Esc/Space/+/-. waitKeyEx thay waitKey cho arrow keys.
- T2.2-T2.4: Overlay functions — draw_countdown (số lớn giữa màn hình), draw_recording (green border + progress bar + [REC] banner), draw_done (success/timeout message).
- T3.1: Balance chart — mini bar chart góc phải, per-class count, green ≥100 / orange <100.
- T3.2: Batch size +/- keys — adjust_batch_size(±10), clamped [5, 200], hiện trên idle overlay.
  **Kết quả:** 62 tests pass (47 new + 12 gesture_state_machine + 3 feature_extractor). App compiles OK. Full guided collection workflow: Tab→select class→Enter→3s countdown→auto-record 30 frames→flush CSV→done message.
  **References:** utils/class_menu.py, utils/collection_manager.py, app.py, tests/unit/test_class_menu.py, tests/unit/test_collection_manager.py

### 18:47 — Check-implementation guided-gesture-collection

**Vấn đề:** Cần verify implementation khớp design + requirements trước khi tiếp tục phát triển.
**Nguyên nhân:** Post-implementation verification step.
**Hành động:**

- So sánh từng file (class_menu.py, collection_manager.py, app.py) với design doc + requirements doc.
- Phát hiện 38 spec points fully met, 5 visual deviations (thiếu "Giữ tay đúng pose!", ● in [REC], ✓/⚠ prefixes, class name in done msg, "NEEDS MORE" text), 2 behavioral deviations (+/- batch size không restrict khi menu mở, DONE_DISPLAY = 1.5s vs 1s).
- Viết 20 integration tests mới tại `tests/integration/test_guided_collection.py`: full workflow, cancel session, data integrity, empty hands, quality gate, timeout flush, hot-reload, class counts sync, rapid session sequence, frame skip + multi-hand.
- Tổng 82 tests pass (0.2s).
  **Kết quả:** Implementation verified correct. Report published tại `docs/ai/implementation/check-implementation-guided-collection.md`. No critical gaps.
  **References:** docs/ai/implementation/check-implementation-guided-collection.md, tests/integration/test_guided_collection.py
