# 12-04-2026 — TouchlessControl

### 17:28 — Fix deviations guided-gesture-collection

**Vấn đề:** Check-implementation phát hiện 7 sai khác giữa implementation và design/requirements.
**Nguyên nhân:** Một số chi tiết visual bị bỏ sót khi implement, logic batch size chưa restrict đúng scope.
**Hành động:**

1. **Batch size guard** (app.py): Thêm `if class_menu.visible:` trước `adjust_batch_size()` — chỉ cho +/- khi menu mở (đúng Requirement #10).
2. **DONE_DISPLAY_SECONDS** (collection_manager.py): Đổi từ 1.5 → 1.0s theo design.
3. **Countdown hint** (app.py): Thêm dòng "Giu tay dung pose!" bên dưới class name trong countdown overlay.
4. **REC indicator** (app.py): Đổi `[REC]` → `[REC ●]`.
5. **Done messages** (app.py + collection_manager.py): Thêm `✓`/`⚠` prefix + "for {class_name}". Lưu `_last_flush_class_id` trong CollectionManager, truyền qua `get_overlay_state()`.
6. **Balance chart** (app.py): Thêm "NEEDS MORE" text (màu cam) cho class có < 100 sample.
7. **Docstring** (collection_manager.py): Sửa "Returns number of frames accepted (0 or 1)" → "Returns 0 or 1 (frame accepted?)".

- Cập nhật test `test_done_state` để verify `class_id` trong overlay done state.
  **Kết quả:** 81 tests pass. Tất cả deviations đã fix, implementation khớp hoàn toàn với design + requirements.
  **References:** app.py, utils/collection_manager.py, tests/unit/test_collection_manager.py
