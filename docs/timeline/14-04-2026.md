# Timeline chi tiết — 14-04-2026

### 19:08 — Tổng kết phiên + tự động hóa tracking

**Vấn đề:** Cần tổng kết đầy đủ phiên làm việc từ đầu tới cuối để các lần làm việc sau có thể đọc lại nhanh tiến trình, lý do thay đổi và kết quả đã xác minh.

**Nguyên nhân:** GitHub chỉ theo dõi mã nguồn, không lưu đầy đủ ngữ cảnh thực hiện, quyết định thiết kế, bug đã xử lý và hướng dẫn vận hành. Trong phiên này cũng phát sinh nhu cầu chuẩn hóa cơ chế theo dõi tiến trình cho agent.

**Hành động:**

- Rà soát lại kế hoạch triển khai pointer control và trạng thái repo hiện tại.
- Hoàn thành chuỗi thay đổi chính cho gesture control:
  - reset vocabulary từ 13 gesture xuống 5 gesture phục vụ điều khiển chuột,
  - tạo `CursorController` + `ScrollController`,
  - nối `GestureStateMachine` + `ActionMapper` vào luồng xử lý chính trong `app.py`,
  - cập nhật config, label CSV và test suite tương ứng.
- Xác minh lại chất lượng sau thay đổi bằng test suite: 111 tests pass.
- Trong bước hướng dẫn Phase 3, phát hiện mismatch đường dẫn dữ liệu thu thập (`keypoint.csv`) so với notebook train (`keypoint_v2.csv`) và đã sửa để pipeline thu thập → train hoạt động đồng nhất.
- Soạn hướng dẫn thủ công cho Phase 3: chạy app ở chế độ an toàn, thu thập mẫu cho 5 lớp, train notebook, rồi kiểm tra live.
- Tạo skill `track-progress` và instruction auto-trigger để agent tự cập nhật timeline sau mỗi tác vụ đáng kể.

**Kết quả:**

- Toàn bộ phiên làm việc đã được tổng kết thành một mốc dễ tra cứu.
- Luồng pointer/scroll control đã hoàn thiện ở mức code + test.
- Pipeline data collection và retraining rõ ràng hơn sau khi sửa CSV path mismatch.
- Agent hiện có cơ chế tracking thụ động, liên tục và có thể đọc lại lịch sử làm việc ở đầu mỗi session.

**Ảnh hưởng:**

- Cải thiện khả năng tiếp nối công việc giữa các phiên.
- Giảm rủi ro mất ngữ cảnh khi chuyển agent hoặc quay lại sau.
- Giúp việc kiểm tra nguyên nhân, hành động và hệ quả của từng thay đổi minh bạch hơn.

**References:**

- `docs/ai/timeline.md` — bảng tổng hợp tiến trình toàn dự án
- `docs/timeline/13-04-2026.md` — mốc triển khai pointer/scroll control trước đó
- `app.py` — sửa đường dẫn CSV và wiring gesture pipeline
- `.github/skills/track-progress/SKILL.md` — skill theo dõi tiến trình
- `.github/instructions/track-progress.instructions.md` — auto-trigger tracking
- `AGENTS.md` — quy định bắt buộc đọc/cập nhật timeline
