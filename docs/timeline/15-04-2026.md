# Timeline chi tiết — 15-04-2026

### 00:45 — Research temporal model thay MLP

**Vấn đề:** Bộ phân loại MLP hiện tại phù hợp cho static pose theo từng frame nhưng chưa đủ để phân biệt tốt các cử chỉ có phụ thuộc thời gian như click ngắn, drag giữ lâu và chuyển trạng thái liên tục.

**Nguyên nhân:** Dự án đang mở rộng từ gesture collection sang touchless mouse control nên cần đánh giá kiến trúc temporal gọn nhẹ, chạy được realtime trên CPU và xuất được TFLite.

**Hành động:**

- Khảo sát các hướng temporal phổ biến cho hand gesture recognition: GRU, LSTM, TCN, CNN-GRU, Transformer.
- Benchmark khả năng triển khai TFLite và độ trễ thực tế trên máy đang dùng.
- So sánh trade-off giữa số tham số, kích thước model, độ trễ và độ phù hợp với bài toán 5 lớp gesture.
- Tổng hợp khuyến nghị triển khai tiếp theo trong báo cáo nghiên cứu.

**Kết quả:** Đề xuất GRU-64 là lựa chọn cân bằng nhất cho realtime touchless control; TCN-3L là phương án dự phòng rất nhỏ và nhanh. Báo cáo đầy đủ đã được lưu trong tài liệu nghiên cứu.

**Ảnh hưởng:** Tạo cơ sở kỹ thuật cho Phase tiếp theo: thu thập dữ liệu chuỗi, huấn luyện temporal classifier và đơn giản hóa state machine hiện tại.

**References:**

- `docs/research/temporal-gesture-model-rnn.md` (new: báo cáo nghiên cứu chi tiết)
- `docs/ai/timeline.md` (modified: thêm mốc research ngày 15-04-2026)
- `docs/timeline/14-04-2026.md` (tham chiếu bối cảnh phiên trước)

---

### 19:15 — Design & refactoring plan MLP→GRU-64

**Vấn đề:** Cần thiết kế chi tiết kiến trúc GRU-64 và lên kế hoạch tái cấu trúc hệ thống từ MLP single-frame sang GRU temporal sequence classifier.

**Hành động:**

- Phân tích toàn bộ codebase (15+ source files, 111 tests) qua sub-agent exploration
- Đọc lại design docs, planning docs, research report
- Thiết kế chi tiết GRU-64 architecture (mechanism, specification, why it solves click-vs-drag)
- So sánh MLP vs GRU-64 trên 12 tiêu chí
- Thiết kế pipeline mới: SequenceBuffer → TemporalClassifier → Debouncer
- Định nghĩa 4 components mới: SequenceBuffer, TemporalClassifier, Debouncer, SequenceCollector (interfaces + specs)
- Thiết kế data format migration: CSV single-frame → NPZ sliding-window sequences
- Lập execution plan 5 phases, 16 tasks (T0.1–T5.3) với dependencies rõ ràng
- Thiết kế backward compatibility: `--model mlp/gru` flag
- Phân tích 8 risks + mitigations
- Rollback plan cho từng phase

**Kết quả:** Tài liệu thiết kế + refactoring plan hoàn chỉnh tại `docs/ai/design/feature-temporal-gru64.md`. Kế hoạch gồm 5 phases (Infrastructure → Core → Training → Integration → Cleanup), ước tính 16–25h dev, thiết kế additive (không xóa code cũ).

**Ảnh hưởng:** Sẵn sàng bắt đầu Phase 1 implementation. Không có code thay đổi — chỉ tài liệu.

**References:**

- `docs/ai/design/feature-temporal-gru64.md` (new: tài liệu chính)
- `docs/research/temporal-gesture-model-rnn.md` (input: benchmark data)
- `docs/ai/design/feature-gesture-prototype-laptop-control.md` (reference: kiến trúc hiện tại)
- `docs/ai/planning/feature-gesture-prototype-laptop-control.md` (reference: M2 milestone)

---

### 20:05 — Implement Phase 1 — 4 core components + 53 tests

**Hành động:**

- Implement `utils/sequence_buffer.py` — Ring buffer (W, 93) với circular overwrite, zero-padding, chronological get_window
- Implement `model/temporal_classifier/temporal_classifier.py` — TFLite wrapper cho GRU-64, (1,W,93)→(class_id, scores)
- Implement `utils/debouncer.py` — Lightweight GestureEvent emitter: confidence threshold + cooldown + edge detection
- Implement `utils/sequence_collector.py` — Continuous recording → sliding window → NPZ append
- Tạo `model/temporal_classifier/__init__.py`, `temporal_classifier_label.csv`
- Viết 53 unit tests: 17 (buffer) + 5 (classifier) + 15 (debouncer) + 16 (collector)
- Fix 2 time-mock test failures (mock time starting at t=0.0 clashed with \_last_event_time=0.0)

**Kết quả:** 164 tests pass (111 existing + 53 new), 0 regressions.

**Files created:**

- `utils/sequence_buffer.py`
- `utils/debouncer.py`
- `utils/sequence_collector.py`
- `model/temporal_classifier/__init__.py`
- `model/temporal_classifier/temporal_classifier.py`
- `model/temporal_classifier/temporal_classifier_label.csv`
- `tests/unit/test_sequence_buffer.py`
- `tests/unit/test_temporal_classifier.py`
- `tests/unit/test_debouncer.py`
- `tests/unit/test_sequence_collector.py`

**References:**

- `docs/ai/design/feature-temporal-gru64.md` (design: Phase 1 spec)
- `docs/ai/planning/feature-temporal-gru64.md` (planning: T1.1–T1.4 marked done)

---

### 22:00 — Phase 2: Training infrastructure

**Hành động:**

- Tạo `scripts/create_dummy_temporal_model.py` — build untrained GRU-64, convert TFLite, verify shapes
- Chạy script → `temporal_classifier.tflite` (91.7 KB, 30853 params, input (1,20,93), output (1,5))
- Verify TemporalClassifier wrapper loads & runs model thành công
- Tạo `temporal_classification.ipynb` — 14 cells: imports, config, NPZ load, class distribution plot, train/val split, build GRU-64, train with EarlyStopping, training curves, confusion matrix + classification report, TFLite export, TFLite verification
- Viết 8 integration tests tại `tests/integration/test_temporal_pipeline.py`: TFLite shape/size/softmax validation + SequenceBuffer→TemporalClassifier end-to-end

**Kết quả:** 172 tests pass (164 old + 8 new), 0 regressions.

**Files created:**

- `scripts/create_dummy_temporal_model.py`
- `model/temporal_classifier/temporal_classifier.tflite` (91.7 KB)
- `model/temporal_classifier/temporal_classifier.keras`
- `temporal_classification.ipynb`
- `tests/integration/test_temporal_pipeline.py`

**References:**

- `docs/ai/planning/feature-temporal-gru64.md` (planning: T2.1–T2.2 marked done)
