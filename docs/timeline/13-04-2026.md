# Timeline chi tiết — 13-04-2026

### 20:19 — Reset 13→5 classes + implement pointer/scroll control

**Vấn đề:** Hệ thống có 13 gesture classes nhưng chưa tích hợp mouse control. Cần thu gọn vocabulary và implement pointer→cursor + scroll control.

**Nguyên nhân:** GestureStateMachine + ActionMapper chưa được wire vào app.py. Không có CursorController cho pointer→cursor mapping. Không có ScrollController cho continuous scroll.

**Hành động:**

- **Phase 1 — Reset vocabulary (T1.1-T1.6):**
  - Overwrite `keypoint_classifier_v2_label.csv`: 13 classes → 5 (null, pointer_move, left_click, drag_hold, scroll_mode)
  - Truncate `keypoint_v2.csv` + `keypoint.csv` (no backup per user)
  - Update `GESTURE_LABELS` in `gesture_state_machine.py` (13→5 entries)
  - Rewrite `gesture_actions.yaml`: only left_click (mouse_click) + drag_hold (mouse_drag); pointer_move + scroll_mode bypass
  - Rewrite `gesture_vocabulary.md` for 5-class definitions
  - Update notebook `NUM_CLASSES = 7 → 5`
  - Add `screeninfo>=0.8` to `requirements.txt`

- **Phase 2 — Implement controllers (T2.1-T2.5):**
  - Created `utils/cursor_controller.py`:
    - `CursorController`: camera coords → screen coords with EMA smoothing (α=0.3), dead zone (5px), padding (8%), screeninfo auto-detect
    - `ScrollController`: continuous Y-delta from anchor point, sensitivity=0.05
  - Added `mouse_move_to(x, y)` + `scroll_by(clicks)` to `ActionMapper` (pynput + ydotool)
  - Wired into `app.py` main loop:
    - `pointer_move` (class 1) → CursorController → mouse_move_to (bypass state machine)
    - `scroll_mode` (class 4) → ScrollController → scroll_by (bypass state machine)
    - `left_click` (class 2) + `drag_hold` (class 3) → GestureStateMachine → ActionMapper.handle
    - No-hand → reset controllers + update_no_hand()
    - Added `--no-actions` flag

- **Phase 4 — Tests:**
  - Created `tests/unit/test_cursor_controller.py` (18 tests): init, update mapping, EMA smoothing, dead zone, padding, reset, ScrollController lifecycle
  - Created `tests/integration/test_pointer_cursor.py` (6 tests): pointer→cursor pipeline, scroll pipeline, click/drag through state machine
  - Updated `tests/test_gesture_state_machine.py`: NUM_CLASSES 13→5, gesture name "open_palm"→"pointer_move"
  - Updated `tests/integration/test_guided_collection.py`: label fixture 5 classes

**Kết quả:** 111 tests pass (0.25s). Toàn bộ Phase 1 + Phase 2 + Phase 4 hoàn thành. Còn lại Phase 3 (data collection + retraining) cần thực hiện thủ công.

**References:**

- `utils/cursor_controller.py` (new)
- `utils/action_mapper.py` (modified: +mouse_move_to, +scroll_by)
- `utils/gesture_state_machine.py` (modified: 5-class GESTURE_LABELS)
- `app.py` (modified: wired controllers + state machine + action mapper)
- `config/gesture_actions.yaml` (rewritten)
- `config/gesture_vocabulary.md` (rewritten)
- `model/keypoint_classifier/keypoint_classifier_v2_label.csv` (5 classes)
- `tests/unit/test_cursor_controller.py` (new, 18 tests)
- `tests/integration/test_pointer_cursor.py` (new, 6 tests)
